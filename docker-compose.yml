version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: ratehonk_postgres
    restart: always
    environment:
      POSTGRES_USER: email_marketing_user
      POSTGRES_PASSWORD: your_secure_password_email_marketing
      POSTGRES_DB: emailmarketing
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: ratehonk_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

volumes:
  postgres_data:
  redis_data:
